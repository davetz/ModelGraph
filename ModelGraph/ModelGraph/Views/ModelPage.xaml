<Page
    x:Class="ModelGraph.Views.ModelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="70*"/>
        </Grid.ColumnDefinitions>

        <Button Content="Button" Click="Button_Click"/>

        <Grid Name="LeftGrid" Grid.Column="0" Background="Pink" Grid.Row="1">
            <TextBlock Text="X" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        </Grid>
        <Grid Name="RightGrid" Grid.Column="1" Background="CornflowerBlue" Grid.Row="1"/>

    </Grid>
</Page>

